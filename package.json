{
  "name": "hutomi",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm --prefix src/frontend install && npm --prefix src/backend install",
    "dev": "concurrently -n frontend,backend -c cyan,green \"npm --prefix src/frontend run dev\" \"node src/backend/server.js\"",
    "frontend": "npm --prefix src/frontend run dev",
    "backend": "node src/backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
